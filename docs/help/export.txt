focus export — back up your data

  focus export [basename]        default: refocus-export-<timestamp>

  .sql    full dump — the reliable restore format
  .json   human-readable and portable (import needs jq)

Reads only; your live data isn't touched.
