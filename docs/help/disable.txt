focus disable — turn tracking off

  focus disable

Silences the nudge and marks tracking off. Your sessions are untouched;
'focus enable' brings it back.

Won't run while a session is open — a live timer with no reminders is the
state that loses you an afternoon. Close it with 'focus off' first.
