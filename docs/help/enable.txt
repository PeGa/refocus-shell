focus enable — turn tracking on

  focus enable

Marks tracking active and arms the nudge schedule. Install does this for you;
you'll only need it after a disable, reset, or import.

Already on? It says so and changes nothing. Nudging then fires in every state,
every NUDGE_INTERVAL minutes (1–60).
